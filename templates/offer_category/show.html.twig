{% extends 'base.html.twig' %}

{% block title %}OfferCategory{% endblock %}

{% block body %}
    <h1>OfferCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offer_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ offer_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offer_category_index') }}">back to list</a>

    <a href="{{ path('offer_category_edit', {'id': offer_category.id}) }}">edit</a>

    {{ include('offer_category/_delete_form.html.twig') }}
{% endblock %}
